import org.openqa.selenium.WebDriver;
import org.openqa.selenium.edge.EdgeDriver;

public class Driver {
	public static void main(String[] args) {
		
		//PROVIDED THE LOCATION OF EDGE DRIVER
		System.setProperty("webdriver.edge.driver", "E:\\Selenium\\msedgedriver.exe");
		
		//EDGE DRIVER INVOKED
		WebDriver driver = new EdgeDriver();
		
		/*
		 * 1. This method will launch a new browser and opens the given URL in the browser instance.
		 */
		driver.get("https://rahulshettyacademy.com/");
		
		String title = driver.getTitle();
		System.out.println(title);
		
		if(title.equals("Selenium, API Testing, Software Testing &amp; More QA Tutorials  | Rahul Shetty Academy")) {
			System.out.println("Correct title");
		} else {
			System.out.println("Incorrect title");
		}
		
		
		//WebDriver driver = new ChromeDriver();
		/*
		driver.close();
		//driver.findElement(null);
		//driver.findElements(null)
		driver.getCurrentUrl();
		driver.getPageSource();
		driver.getTitle();
		driver.getWindowHandle();
		driver.getWindowHandles();
		driver.manage();
		//driver.navigate()
		driver.quit();
		//driver.switchTo()*/
	}

}
